name: Contract and Adapter Conformance

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements-dev.txt
      - run: python3 compiler/compile_adapters.py --check
      - run: python3 conformance/validate_contract.py
      - run: python3 -m unittest discover -s tests -v
