# T4H Codex adapter

Follow the canonical material under `contract/`. Files under `adapters/*/generated/` are compiler output and must not be edited manually. Enforce approval, idempotency, retry, recovery, result and receipt requirements from the contract. Runtime evidence overrides intended configuration.
