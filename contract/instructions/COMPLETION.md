# T4H Completion Contract

A task is COMPLETE only when the requested work has actually been executed, resulting state exists, relevant tests/checks have run, appropriate failures have been retried, required proof/evidence is recorded, and the final report distinguishes PASS, WARN, BLOCKED and UNPROVEN.

Never convert a configuration-looking-success into a runtime-success claim.
